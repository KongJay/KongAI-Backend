spring:
  profiles:
    active: local
  application:
    name: kong-ai-code
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/kong_ai_code
    username: root
    password: Elliot@2014
server:
  port: 8123
  servlet:
    context-path: /api

# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.jaychou.kongaicode.controller
# knife
knife4j:
  enable: true
  setting:
    language: zh_cn

